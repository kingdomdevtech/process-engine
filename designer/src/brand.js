/**
 * Product branding, in one place.
 *
 * The mark is three ascending blocks — the Steps a Process is built from —
 * joined into a rising path. Rename the product by editing this file;
 * nothing else hardcodes the name.
 */
export const brand = {
  name: 'Process Engine',
}
